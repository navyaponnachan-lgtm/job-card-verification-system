config.json
verification.db
__pycache__/
*.pyc
build/
dist/
*.spec
installer_output/
.claude/settings.local.json

# real client photos used only for local testing - contain a real
# person's name, phone number, and amounts; not meant for the public repo
1000138302.jpg
1000138303.jpg

# screenshot taken from a real run - shows the same real client's name/data
ppt_screenshot_ledgerlens.png

# mentor-facing progress decks/scripts - kept locally only, not part of
# the public repo
*.pptx
*.pdf

# a local backup/reference copy of this same project (has its own
# config.json with a real API key and real sample images inside it) -
# never meant to be committed
/verification_system/
